# OuVoice sovereignty contract

```bash
pnpm install
pnpm --filter @ouvoice/contracts build
```

`OuVoiceAssetNFT` uses OpenZeppelin Contracts 5.0.2 and Solidity 0.8.20. Deploy it with the platform multisig address as `initialAdmin`. Ownership transfer uses OpenZeppelin's two-step acceptance flow.
