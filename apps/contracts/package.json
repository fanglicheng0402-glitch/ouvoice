{
  "name": "@ouvoice/contracts",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf artifacts && solcjs --base-path . --include-path node_modules --bin --abi contracts/OuVoiceAssetNFT.sol -o artifacts",
    "typecheck": "pnpm build",
    "test": "pnpm build"
  },
  "dependencies": {
    "@openzeppelin/contracts": "5.0.2"
  },
  "devDependencies": {
    "solc": "0.8.20"
  }
}
